import { RemoveDupPipe } from './remove-dup.pipe';

describe('RemoveDupPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDupPipe();
    expect(pipe).toBeTruthy();
  });
});
